{
  "name": "@urbanflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "UrbanFlow Mobility - shared TypeScript contracts (DTO interfaces, itinerary types) used by apps/web and apps/api",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare": "npm run build"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,mjs,json,md}": "prettier --write"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

/**
 * Rôle d'un compte (UF-701) — contrôle d'accès basé sur les rôles (C4).
 * Défini dans `@urbanflow/shared` (vocabulaire commun front/back — C9) et
 * ré-exporté ici pour conserver des imports relatifs courts dans les modules,
 * comme `route-priority.enum.ts` et `transport-mode.enum.ts`.
 */
export { DEFAULT_USER_ROLE, UserRole } from '@urbanflow/shared';
